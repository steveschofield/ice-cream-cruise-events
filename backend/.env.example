# Database connection
DATABASE_URL=postgresql://user:password@localhost:5432/ice_cream_cruise

# Admin authentication (required)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=changeme123

# Server port (optional, defaults to 3000)
PORT=3000
